'use client';
import { BlogCardGroup } from '@/features/blog/components';
import { Container } from 'react-bootstrap';

const page = () => {
  return (
    <Container>
      <BlogCardGroup />
    </Container>
  );
};

export default page;
